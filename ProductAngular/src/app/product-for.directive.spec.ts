import { ProductForDirective } from './product-for.directive';

describe('ProductForDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductForDirective();
    expect(directive).toBeTruthy();
  });
});
